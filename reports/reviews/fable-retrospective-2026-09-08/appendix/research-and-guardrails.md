# Research grounding and operational guardrails

Inspector: research-and-guardrails. Date: 8 September 2026. Inputs: frozen snapshot under `/Users/arunmenon/projects/adrl-review-fable-2026-09-08/snapshot/` (register R, runtime C, both personal skill copies). Read-only; no repository file, scheduler state or automation file was modified. Primary sources were fetched with the web tool on 8 September 2026 unless marked search-only.

Method. I started from the four source ledgers (the 7 September independent review, its manifest, the artifact source check, BIBLIOGRAPHY.md), then read the documents that changed or proposed decisions on 7 and 8 September, then the ADRs those documents amended (found through CHANGELOG.md), then re-fetched the primary sources behind the highest-impact claims. "Accurate" below means the register or report states what the source says; it is not a judgement that the source is right. For every claim I record whether an earlier register ledger marked it fetched or search-only.

## Part A. Claim verification

| # | Claim | Used in | Decision that depends on it | Primary source fetched (date, version, method) | Accuracy of the register's statement | Counterevidence or limits in the source | Applicability to ADRL | Earlier ledger marker |
|---|---|---|---|---|---|---|---|---|
| A1 | Escalation from a cheap to a strong model "recovers less than half of the LC-to-HC quality gap while incurring a substantial cost premium"; downshifting is favourable ("Handoff Tax") | critique 3.1, and RTG-004, CAS-004/005 attacks (critique lines 730-989); independent review disposition 1; blueprint section 9 keeps RTG-004/CAS-005 as baseline | Whether conditional local-first (RTG-004) and sticky-up (CAS-005) stay the baseline or are replaced | Yes. arXiv 2608.24358, v1 submitted 25 Aug 2026 (abstract page and HTML full text). 500 SWE-bench Verified tasks, 58,000 runs, Claude Haiku 4.5 to Opus 4.7 and GPT-5.6 Luna to Sol, switch points fixed at calibrated percentiles p5-p50, one episode per task and configuration, LiteLLM v1.83.14 pricing with cache reads and writes | Critique quotations are verbatim and accurate. Independent review's characterisation (two pairs, 500 tasks, 58,000 runs, fixed switch points, one rollout, pricing dependence) is accurate | Authors: single episode per configuration, no variance estimate; hard stratum N about 24-27 per cell "exploratory rather than a cross-family finding". No local or open-weight model; switch points fixed, not triggered by observed trouble | Moderate. ADRL's local rung is not the tested LC model and escalation is trip-wire triggered (CAS-001), which the paper does not test. Supports adding compaction and downshift arms to E06, not replacing RTG-004 | critique: fetched; independent review: full text consulted; not cited in any ADR |
| A2 | 21 routers over 5 benchmarks converge to a plateau; only trajectory-conditioned routing escapes it ("Routing Plateau") | critique 3.2; RTG-007 and EVL-003 evidence lists; independent review disposition 2; blueprint section 4-5 | Whether RTG-002/003/006/007 must move to a trajectory-conditioned learned router | Yes. arXiv 2606.07587, v1 27 May 2026, HTML. 21 methods, 5 benchmarks (RouterBench, R2-Bench, EmbedLLM, CARROT/SPROUT, new 30k/300k set). Appendix B limits scope to single-shot routing and excludes cascades, dynamic pools and multi-turn agentic routing | Independent review accurate. The critique's "only trajectory-conditioned routing escapes" is not stated by the paper; the paper's own escape levers are data (+0.47 pp), encoder (+0.13 pp) and fine-tuning (+0.42 pp), total +1.24 pp, about 14.6% of the oracle gap | kNN is top-2 on all five benchmarks, which supports simple baselines; the levers close little of the gap | High for the negative lesson (do not expect a learned prompt router to beat a strong fixed model). Low for W7.0a, which is a lexical rule not a learned router | RTG-007/EVL-003/BIBLIOGRAPHY: "not independently fetched"; independent review: full text. The ADR marker is stale |
| A3 | Probe-then-escalate on a partial trajectory is Bayes-better than routing on the prompt alone ("SWE-Router") | RTG-007 proposed amendment (pending disposition); blueprint anchors the three-loop target on RTG-007; critique 3.2 | The RTG-007 disposition and the blueprint's milestone 3 design | Yes. arXiv 2607.00053, v1 30 June 2026, abstract. Theorem: conditioning on the partial trajectory never harms and is strictly better when exploration is informative; empirical multi-LLM trajectory dataset | Register and review statements accurate | The theorem is about information, not a finite estimator; the probe's cost and side effects are not bounded by the theorem (the review's 0.1/0.9 counterexample is valid) | Moderate. ADRL already escalates at a boundary on trip-wires; no continue/switch estimator exists. Supports the blueprint's "trajectory-capable" wording | RTG-007/BIBLIOGRAPHY: not independently fetched; independent review: full text |
| A4 | Adding task-level performance statistics to a vanilla LLM router gives a 15.3% relative gain ("Agent-as-a-Router") | critique finding 3 (memory must feed the router); RTG-001, RTG-007 evidence lists; independent review disposition 3 | Whether MEM-008's advisory gate and LRN-007's graduation should be weakened | Yes. arXiv 2606.22902, v3 26 June 2026, HTML. Table 1: Vanilla 41.41, +Perf stats 47.74, DimensionBest heuristic 47.50, oracle 57.00; 15.3% is computed against Vanilla. Section 5.3: cache hit rates unobservable, so cost is a secondary metric | Independent review's correction is exactly right: the gain over the statistics heuristic is 0.24 points, about 0.5%. No ADR carries the 15.3% figure (grep of adr/ for "15.3" returns nothing) | CodeRouterBench is about 10K tasks over 8 frontier LLMs; no local models | Supports keeping simple statistics as a serious baseline (blueprint section 4); does not support removing MEM-008's gate | RTG-001/007: not independently fetched; independent review: full text |
| A5 | Multi-turn history-conditioned routing beats GPT-5 at 43-59% lower cost ("MTRouter") | RTG-001, RTG-007 evidence lists (from the critique) | Same as A3 | Yes, abstract only. arXiv 2604.23530, v1 26 April 2026. Benchmarks ScienceWorld and Humanity's Last Exam; 58.7% and 43.4% cost reduction versus GPT-5 | Register cites title only; no figure is quoted in the ADRs I read | Not a coding-agent benchmark; needs logged trajectories for training | Low. Different task domain; useful only as design precedent | Not fetched by anyone before this review; still abstract-only |
| A6 | Replaying a switched model against the original log "scores the wrong world"; 74-77% of early swaps diverge at the first post-fork action ("Replay Gap") | LRN-002/003/007/008, MEM-009, EVL-001/003, LRN and EVL bucket READMEs; blueprint section 5; product roadmap; critique | EVL-003 (replay is prohibited as evidence); blueprint milestone 3 (independent branches) | Yes. arXiv 2608.08239, v1 8 Aug 2026, HTML. 30 instances per run pair, Qwen3-4B (FP8) and Qwen3-14B (AWQ) on one 24 GB GPU, 28k context, base resolution 0-3%, forks at 30% and 70%, about 900 rollouts, 717 scored pairs. Router recommendations are "hypotheses our regime motivates but cannot confirm" | Register's 74-77% and "predicted none of the observed outcome flips" are accurate; independent review's limits accurate; blueprint's abstract-level use accurate | FP8-served controls diverged in over 90% of forks, so part of the divergence is serving nondeterminism; task resolution near zero | High for evaluation discipline (EVL-003). The routing lab replays synthetic traces as a mechanism check only, which the lab report states | BIBLIOGRAPHY: not independently fetched (09-03 citation); 09-02 review cited details; independent review: full text; blueprint: abstract |
| A7 | Several routing methods, including commercial routers, fail to beat a Best Single baseline; adding models has diminishing returns ("LLMRouterBench") | RTG-001/003/007; blueprint section 5; product roadmap | Keep a fixed strong model as a serious competitor in every comparison | Yes, abstract. arXiv 2601.07206, v1 12 Jan 2026. 400K instances, 21 datasets, 33 models | Accurate. The RTG-007 figure "OpenRouter minus 24.7% versus Best Single" was not rechecked (abstract only) | None in abstract | High for experiment design | Blueprint ledger: abstract; earlier register: HTML v1 cited |
| A8 | Routing decomposes into five components; no single router dominates; multi-round routing does not consistently help ("LLMRouter") | blueprint section 4-5; product roadmap | Blueprint's modular selector design | Yes. arXiv 2608.06867, v1 7 Aug 2026, HTML. Quotes: "No single router dominates across all tasks"; "Multi-turn routing does not consistently outperform single-turn routing"; learned routers +14.6% relative over static baselines on xRouteBench | Accurate, including the learned-router gain the blueprint mentions | Multi-round routers score far lower (about 22-23 versus about 45) in Table 2 | Moderate; benchmark is not coding agents | Blueprint ledger: full text |
| A9 | Thinking signatures bind a block to the producing model and to the conversation prefix; prefix binding is enforced for accounts created on or after 31 Aug 2026; edits inside the prefix yield 400 unless drop_block is set | critique 3.4; CAS-004/006, RTG-008 (amended 09-02/09-03); independent review disposition 4 | CAS-004's pair table and RTG-008's gateway contract | Yes. platform.claude.com thinking page. A model reads its own blocks and those of earlier models, never newer ones; unreadable blocks are dropped silently and unbilled. A block is valid only while system, tools and preceding messages are unchanged; on mismatch the API returns 400 or drops, per `thinking.block_binding.prefix_mismatch_behavior`. Prefix check enforced by default for accounts created on or after 31 Aug 2026 00:00 UTC; beta `thinking-binding-controls-2026-08-01` adds `input_transformations`. `redacted_thinking` must be echoed unchanged | Critique and ADR quotations accurate | The model check is per model version, not per family: Opus 5 cannot read Fable 5.1 blocks although both are Anthropic. CAS-004 clause 1 and `src/adrl/cascade/handoff.py:3` key the rule by family | High. Any ADRL-side rewrite of system, tools or earlier messages (handoff transformation, injected reminders) invalidates every later thinking block for new accounts. No snapshot test exercises `block_binding` or records `input_transformations` (finding F2) | 09-02 review: fetched; independent review: D2 fetched |
| A10 | A same-provider flaw let weaker models decode stronger models' reasoning; 315,320 blocks and 62 API keys recovered | critique 3.4 and CAS-005 attack; independent review said the counts were not independently established | Motivation only; the review says the amendment does not need it | Search-only. Reports by The Hacker News, CSA research note (14 Aug 2026), Implicator, TechTimes: 315,320 blocks across 6,708 public trajectories, 62 API keys, preprint 10 Aug 2026; vendors state the attack no longer reproduces | Counts match the news reporting; preprint not fetched | Vendors report mitigation; the register does not need the figures | Low after mitigation; strip-on-downshift remains sensible | Critique: fetched (news article); this review: search-only |
| A11 | Server-side compaction can occur inside a response rather than as a separate utility request | independent review disposition 5; SEM-001/004/007 (SEM-004 cites the page "title from search, not fetched") | SEM-001 request classes; SEM-004 content-bearing utility rule; cost accounting | Yes. platform.claude.com compaction page. Enabled by `context_management` edit `compact_20260112` under beta `compact-2026-01-12`; the summary arrives as a `compaction` content block in the response and is sent back on later requests; top-level `input_tokens`/`output_tokens` exclude the compaction iteration, totals must be summed over `usage.iterations` | Review accurate. SEM-004's rationale still describes only the harness-side "ask a model to summarise" shape | None | High. Two consequences: the compaction fixture in SEM-004 follow-ups must include the in-response shape, and ADRL's usage accounting (`src/adrl/core/types.py:347` combines `message_start` and `message_delta` usage) will undercount cost unless it sums `usage.iterations` (finding F3) | SEM-004: search-only; independent review: fetched |
| A12 | Claude Code sends identifiable headers and requests through a gateway, classifier traffic is distinguishable, behaviour varies by version | independent review disposition 5; FND-003, SEM-001/002/003/006 (cited in 09-02 review) | SEM fingerprints and the "inspect without modifying" boundary | Yes. code.claude.com gateway compatibility guide. Headers `x-claude-code-session-id`, `x-claude-code-agent-id`, `x-claude-code-parent-agent-id`; `anthropic-beta` must be forwarded verbatim as an open list; "A gateway that rewrites or redacts request bodies for content inspection breaks the pairing the same way stripping does, so inspect without modifying"; auto-mode classifier requests carry only the attribution block as the Claude Code marker; behaviour changes at v2.1.181, v2.1.229, v2.1.248 | Review accurate | Vendor guidance is directly against body rewriting; ADRL's handoff and pin rewrites are by design an exception the vendor does not support | High. Confirms FND-001's "unchanged path forwards original bytes" is the supported case and every rewrite is unsupported territory; fixtures must track versions | 09-02 review: fetched; independent review: D3 fetched |
| A13 | Responses state: `previous_response_id` retrieves earlier context server-side; `store` defaults to true (30 days); Codex custom providers use `wire_api = responses` only | runtime `docs/responses-admission.md`, `docs/protocol-boundary.md`; SEM-007 clause 3; multi-harness contract | Responses admission scope (reject referenced state, require store=false) | Yes. developers.openai.com conversation-state guide (chained input billed as input tokens; responses stored 30 days by default; `store: false` disables; conversation objects have no TTL; encrypted reasoning items; compaction via `context_management` and `/responses/compact`). learn.chatgpt.com config reference: "responses is the only supported value"; `supports_websockets` flag | Runtime docs accurate; the SEM-007 statement that request-local scanning cannot see referenced history is correct | None | High. The admission document's reject-by-default rule is the only rule consistent with the source | 09-02 review: fetched (conversation state); independent review: D8 fetched |
| A14 | Streamed LiteLLM responses carry no served-identity header (critique); fixed by PR #7263 in Dec 2024 (independent review) | critique 3.7 and OPS-006/TRU-002/RTG-008 attacks; independent review disposition 7 | OPS-006 blocker; TRU-002 receipt confidence; whether streamed rows are `assumed_intended` | Yes. Issue #7249 opened 15 Dec 2024: `x-litellm-model-api-base` missing on chunked responses; PR #7263 merged 17 Dec 2024 with commit "return api base in streaming response". LiteLLM response-headers page lists `x-litellm-model-id`, `x-litellm-model-group`, `x-litellm-model-api-base` and says only the cost component headers are non-streaming only | Independent review accurate about the issue and the fix | The runtime does not use `x-litellm-model-api-base`. It keys served identity on `x-litellm-model-id` (`config/rungs.yaml:10`, `src/adrl/config/models.py:59`, `src/adrl/wire/observe.py:76`), with fallback to the body model then `assumed_intended`. Neither source establishes whether `x-litellm-model-id` is emitted on streams by the pinned LiteLLM "1.91" (`config/provider-pairs.yaml:3`) | Both the critique's blocker and the review's rebuttal argue about a header ADRL does not read. The OPS-006 D2 claim ("from the gateway header ... under unit and end-to-end tests") is not evidence about a real streamed LiteLLM response (finding F1) | Critique: issue search-only, docs fetched; independent review: D4 fetched |
| A15 | The Claude Code sandbox had four escapes in nine months and Anthropic recommends a VM for untrusted repositories | critique 3.6; SAF-007 (superseded), OPS-001/003; independent review disposition 6 | SAF-007 replacement's isolation boundary and the "sandbox actually on" probe | Vendor page fetched: "Work on an untrusted repository: a dedicated virtual machine, or Claude Code on the web"; sandboxed Bash covers only Bash; Linux runtime deny list built once at launch; "Don't take a clean start as proof your settings loaded". CVEs search-only: CVE-2025-66479 (empty allowlist read as allow-all, fixed v2.0.55 on 26 Nov 2025, assigned to sandbox-runtime), CVE-2026-39861 (symlink following, fixed 2.1.64), CVE-2026-55607 (worktree named `.git`, versions 2.1.38-2.1.162, fixed 2.1.163, GHSA-7835-87q9-rgvv) | Three of the four escapes confirmed; the SOCKS5 null-byte bypass was not rechecked. Vendor recommendation quoted accurately | The CVEs concern Claude Code's own sandbox; ADRL's W3 isolation candidate is a Docker/LinuxKit container (W3.2b2c), and its verifier path uses Seatbelt on macOS (session-verification report) | Moderate. The vendor's own "clean start is not proof" line is the strongest support for OPS-008's active probe | Critique: GitLab advisory fetched, others search; independent review: D7 fetched |
| A16 | EDPB says do not register clear-text, encrypted or hashed personal data on an immutable ledger; keyed hashes remain personal data; key destruction can render data unlinkable | critique on MEM-001/005/010; independent review disposition 10; MEM-010 pending amendment | MEM-010's crypto-shredding versus store-by-reference | Yes. EDPB Guidelines 02/2025, version 2.0, "Adopted on 07 July 2026" (PDF text extracted). Para 104: "not advisable to register personal data in those forms on a blockchain. Instead ... stored off-chain." Para 52: a keyed hash "will also be considered personal data"; after deletion of the key or salt the hash "should not be linkable", subject to unbroken algorithms and unleaked keys; unsalted hashes insufficient. Para 103: consider other tools if strong integrity is not needed | Critique accurate on both points; independent review's "requires separate analysis" is fair because the guidance is blockchain-specific | The guidance addresses distributed, often public, ledgers; ADRL's ledger is a local SQLite stream with backups | Moderate. Para 104's reasoning (erasure is hard on an immutable store) transfers to any append-only store that is backed up; MEM-010's per-session keys match para 52's conditions only if key destruction is proven across backups, which W3 evidence says is unqualified | Critique: "S" (search) for the PDF, "F" for the news page; independent review: D6 fetched; this review: PDF fetched and text-extracted |
| A17 | Soft-deleted vectors remain reconstructible from HNSW index files; key-discard deletion reduced PII recovery to 0% ("Ghost Vectors") | critique on MEM-005/007/010; independent review disposition 10 | MEM-010 erasure proof must cover projection files and backups | Yes, abstract. arXiv 2606.18497, v1 16 June 2026. Three HNSW implementations; 25.5% exact names and 46.4% locations (Wikipedia text), 100% patient age and gender, 99% face identity; Epoch Key Rotation 0% recovery, 500 vectors deleted in 2.5 ms | Critique's numbers accurate; review's "varies sharply by data type" accurate | Data- and encoder-dependent; the tested systems are not ADRL's NumPy index | Moderate. The "old file still holds the vector" mechanism applies to any file-backed index and to backups | Critique: fetched; independent review: full text |
| A18 | Textual embeddings can be inverted with zero training data and DP defences fail ("Zero2Text") | critique on MEM-005; independent review disposition 10 | Embeddings stay prompt-class (MEM-005 replacement) | Yes, abstract. arXiv 2602.01757, v1 2 Feb 2026, v2 3 Feb 2026. Black-box, cross-domain; 1.8x ROUGE-L and 6.4x BLEU-2 over baselines on MS MARCO against an OpenAI encoder | Accurate | Recovery rates are relative, not absolute; encoder unnamed in abstract | High for the classification decision already taken | Critique: fetched; independent review: abstract |
| A19 | A preference-conditioned direct policy trained from bandit feedback beats offline routers by at least 12.46% ("BaRP") | independent review disposition 3 (LRN-003 should not mandate a CATE estimator) | LRN-003 algorithm neutrality | Yes, abstract. arXiv 2510.07429, v1 8 Oct 2025. Single-step routing, simulated bandit feedback from logs, test-time preference dial | Accurate | Static logs, single-step | Moderate for the "let policies compete" recommendation | Independent review: full text |
| A20 | Not Diamond Code claims "frontier-level quality at 20%+ lower inference cost", gated waitlist, local proxy sends only metadata | product roadmap section on competitors (C1-C4) | Positioning only | Yes. code.notdiamond.ai docs | Roadmap labels it a vendor claim; accurate | No method or benchmark published | Positioning; not a design input | Roadmap ledger: fetched |
| A21 | LiteLLM's reported savings price a hypothetical baseline request and deduct the classifier charge | product roadmap | Roadmap's insistence on executed alternatives | Yes. docs.litellm.ai auto_routing: baseline is the most expensive model in the hardest tier on a fixed 20k-token reference request minus the classifier call | Accurate | Not a completed-task comparison, as the roadmap says | Supports EVL-001 comparator rules | Roadmap ledger: fetched |

Verified without a new fetch: the manifest signing code exists (`src/adrl/learning/artifacts.py`, sign and verify Ed25519 graduation record), consistent with the review's disposition 14.

### Claims I did not recheck, and why

- MTRouter beyond the abstract, RouterBench 2024 figures, and RTG-007's "OpenRouter minus 24.7%" figure: abstract-level use only, no 7-8 September decision changes on them.
- The SOCKS5 null-byte bypass, the `sandbox-exec` deprecation notice and the CVE-2026-35020 to 35022 settings chain: cited by the 3 September critique, not by any document amended on 7 or 8 September; three of the four escape CVEs were confirmed instead.
- The 10 August 2026 preprint behind the reasoning-block decoding incident: news reports and the CSA note were sufficient for the register's motivational use; the vendors report mitigation.
- Darwin Godel Machine, AlphaEvolve, Context Graph, Zep, RepoGraph, GraphRAG, DORA, METR and the MT-Bench judge study: cited by the blueprint, context-graph proposal, lab-assessment design and roadmap at abstract or documentation level; those documents change no decision text, and each carries a per-source access scope in its ledger. Re-fetching them would not change any current gate.
- Independent review sources S9-S17 (opportunity versus realizability, resolution diagnostics, CLT position paper, SlopCodeBench, CaMeL, IFC, APPA, secret-detection papers): outside the priority list and not relied on by a 7-8 September decision.
- OpenCode, Portkey, Copilot Auto, LiteLLM Ollama and the Claude Code billing page: positioning and integration feasibility only.
- The 121-source 2 September bibliography: not re-audited, as the independent review also states.

## Part B. Operational guardrails

| Boundary | Where asserted | Proof found in the snapshot | Violation |
|---|---|---|---|
| No paid API budget | `reports/research/adrl-execution-state.json` `paid_api_budget_approved: false`; every wave JSON `new_paid_usage: false`; automation prompt "do not spend new API funds" | Validators assert the flag (`routing-correction-2026-09-08/validate.py`). No API-key spend evidence. The skill smoke and recheck runs record `total_cost_usd` 0.312 and 0.288 (list price) for two `claude --print` calls; the handoff says these are usage metadata, not billing proof. The credential mode (subscription versus API key) is not recorded in `skill-smoke-metadata.json` | None found; proof of subscription billing for the two CLI calls is missing |
| No live routing exposure | state `live_routing_exposure_approved: false`; roadmap gates | Lab and correction runs dispatch to a synthetic endpoint (`lab-final/results.json`, `model_calls: 0`); null rung evidence references block `routing=live` (course-correction plan) | None |
| No automatic graduation | state `automatic_graduation: false`; LRN-007, EVL-007 | `src/adrl/learning/artifacts.py:114-133` `sign_graduation` "is a human action, never automated"; `load_graduated` (line 206) refuses without a verified Ed25519 graduation record and policy compatibility; `readiness.py:162` adds a blocker when no graduated artifact; `app.py:135-165` loads only a graduated exploration artifact and W7.0a adds a features-version mismatch rejection. No CLI path signs a record | None |
| Independent reviewer | state `independent_reviewer_assigned: false`; register AGENTS.md checkpoint (added 8 Sep) requires pre-wave and post-wave independent review before claiming a wave complete | Handoff README: the checkpoint and skill "were added during handoff preparation and are not evidence that earlier waves were independently reviewed"; dispositions: "The full two-day review remains not started". All W0/W3/W7 slices carry `engineering_state: validated*`, never the skill's `reviewed` vocabulary | None; the field is honest. Note that "validated" is Codex self-review by the automation prompt's own definition (journey line 57) |
| Automation paused | state `automation.status: paused`, id `continue-adrl-implementation-waves`, hourly heartbeat | `~/.codex/automations/continue-adrl-implementation-waves/automation.toml`: `status = "PAUSED"`, `rrule = "FREQ=HOURLY;INTERVAL=1"`, `kind = "heartbeat"`, created 2026-09-07T18:12:07Z, updated 2026-09-08T06:32:34Z, matching the journey's pause at entry 016 and the state's `deferred_at` 06:29:47Z. The validator reads this file and asserts PAUSED. No crontab, no ADRL launchd agent, no Claude Code cron job (`CronList` empty), no ADRL process running | None. The pause is a mutable Codex app state file, checked only when a validator runs |
| Run limits: three slices per run, three failed repairs per task, no unchanged retries | state `run_limits`; W0 packet line 16; automation prompt; correction contract; wave packets | W3.2b2b2: "35 passed after three repair cycles" (at the limit). W3.2b2d2: `run_limit_reached: true`, `research_failed_runs: 2`, no third run. W7.0a: `repair_cycles: 1`. Deviations recorded in `adrl-w3-isolated-launch-2026-09-08.md`: the second pytest invocation "continued three later cases despite the stop rule"; "an unchanged run was accidentally started after an edit guard refused and was interrupted"; a never-started container removed "under a documented one-off operator-maintenance exception" | Two recorded deviations from the packet's own stop and no-unchanged-retry rules, and one self-granted exception, in a slice still closed as `validated_pinned_synthetic_engine` (finding F6) |
| Failed candidates and disqualified runs retained | wave template "record costs for failed and discarded candidates"; skill post-wave scope | `routing-correction-2026-09-08/candidate-1/` holds the rejected `features.py` (sha 61e659...) beside the final; `checks-1/tests.log` ends "1 failed, 910 passed, 8 skipped" on the sticky-cascade case. `routing-lab-2026-09-08/run-1/QUALIFICATION.md` explains the overwritten `source` field and keeps run 1. `product-roadmap-2026-09-08/validation-attempt-1.json` keeps the failed link check. Launch failures are embedded in `adrl-w3-2b2d2-launch-contract-2026-09-08.json` (`research_runs`); `launch-recovery-2026-09-08/` holds only the probe script. The isolated-launch report admits the underlying exception of the lost create "was not retained" | None for candidates and runs; one raw failure cause not retained (acknowledged) |
| Validators do not rewrite history | review prompt warns some validators overwrite evidence | `check_all.py` requires a fresh output directory outside the repo (`exist_ok=False`). `run_routing_lab.py` and `matrix_driver.py` open outputs with mode `x`. `routing-correction-2026-09-08/validate.py:98` and `product-roadmap-2026-09-08/validate.py:128` overwrite `validation.json` with `write_text`; both depend on ambient paths (`.adrl-execution-state` backups, `.codex-executive-build`, the Codex automation file). No validator writes into ADRs, patches or wave JSONs | None; validation records are overwritable and machine-bound (finding F7) |
| Source and evidence ownership | journey line 57: Codex implements and self-reviews; user is product owner; reviewers unappointed | Every report, patch, state and validator is Codex-authored by the automation prompt; the two review texts carry Claude session IDs and model usage in `skill-smoke-review.json` and `skill-recheck.json`; both skill copies were written by Codex. No per-file author field exists | None |
| Skill copies identical | `references/handoff.md`: "Keep both copies identical when revising shared instructions" | `diff -r skill-claude skill-codex`: `SKILL.md` and `references/handoff.md` byte-identical (manifest sha 363e8a... and d53d79...); only `agents/openai.yaml` (261 bytes of Codex interface metadata) differs. Live copies match the snapshot | None on shared instructions |
| Register AGENTS.md checkpoint consistent with the skill | register `AGENTS.md` "Independent review checkpoints" | Paths and scopes match the skill. Runtime `AGENTS.md` carries no checkpoint text although the skill says to read both repositories' instructions. `handoff.md` prescribes `reports/reviews/` for packets; the first handoff lives under `reports/research/independent-review-handoff-2026-09-08/` | Minor inconsistency (finding F8) |
| Unresolved review gates in the handoff folder | `skill-smoke-review.md`, `skill-dispositions.md`, `skill-recheck.md` | Four material flaws raised; three accepted, mandatory commit rejected with reason; recheck: no remaining blockers within scope; the two nonblocking gaps (identity limit, secrets exclusions) were added to `handoff.md` (last paragraph, verified). Both runs: `permission_denials: []`, no tools, `web_fetch_requests: 0` | None; scope was the skill text only, as both files state |

## Part C. Findings

**F1. The served-identity receipt argument is about a header ADRL does not read.** Severity medium, confidence high. Claim: OPS-006 records served identity "from the gateway header" at D2; the critique says the header is absent on streams; the review says the bug was fixed in December 2024. Observation: the runtime reads `x-litellm-model-id` (`config/rungs.yaml:10`, `src/adrl/wire/observe.py:76`), while issue #7249 and PR #7263 concern `x-litellm-model-api-base`; the LiteLLM headers page is silent about `x-litellm-model-id` on streams. Neither side has evidence about the deployed header. Acceptance: one streamed Messages request through the pinned LiteLLM 1.91 shows `x-litellm-model-id` on the response headers, and the register reports the measured share of streamed rows with `served_source = gateway_reported` versus `assumed_intended`.

**F2. Thinking-block binding is per model version and per prefix, and no snapshot test covers it.** Severity medium, confidence high. Claim: CAS-004 keys the reasoning rule by provider family; RTG-008 says the gateway "never modifies the prefix inside a rung". Observation: the vendor page says a block is readable by the producing model or a newer one, dropped silently otherwise, and invalidated by any change to system, tools or preceding messages, with 400 by default for accounts created on or after 31 August 2026. `src/adrl/cascade/handoff.py:3` keys by family and no test in the snapshot names `block_binding` or `input_transformations`. Acceptance: a fixture where ADRL's handoff or pin rewrite precedes a request carrying thinking blocks passes with `prefix_mismatch_behavior: drop_block` set and records `input_transformations` on the outcome row; the pair table is keyed by deployment.

**F3. Compaction billing and shape are not reflected in cost accounting or fixtures.** Severity medium, confidence high. Claim: SEM-004 treats compaction as a harness-issued utility request and cites the vendor page as "title from search, not fetched". Observation: server-side compaction arrives as a content block inside an ordinary response, and the top-level usage excludes the compaction iteration; `src/adrl/core/types.py:347` combines `message_start` and `message_delta` usage only. Acceptance: a captured response with `usage.iterations` is added to fixtures, cost equals the sum over iterations, and SEM-004's source marker is updated to fetched.

**F4. Stale "not independently fetched" markers.** Severity low, confidence high. RTG-001, RTG-007, EVL-003, the EVL and LRN READMEs and BIBLIOGRAPHY.md still mark SWE-Router, Agent-as-a-Router, Routing Plateau and Replay Gap as not independently fetched, although the 7 September review consulted the full texts and this review re-fetched them. MTRouter remains abstract-only for everyone. Acceptance: each entry names the ledger that fetched it and the access scope.

**F5. The 15.3% misattribution did not reach the ADRs.** Severity none, confidence high. No ADR quotes the figure; the correction in the independent review is arithmetically right (47.74 versus 47.50). Recorded so the point is not re-litigated.

**F6. Recorded stop-rule deviations closed as validated.** Severity medium, confidence high. Claim: W3.2b2d2 closed `validated_pinned_synthetic_engine` (execution state). Observation: the isolated-launch report records a pytest run continuing three cases after the stop rule, an accidental unchanged rerun, and a removal under a self-declared one-off exception. These are honestly logged, and the maxfail=1 and teardown-halt corrections were added, but the packet's disposition line does not name them as limits, and no product-owner disposition exists for the exception. Acceptance: the slice record lists each deviation with owner disposition, and the next engine allowance shows maxfail=1 in its check manifest.

**F7. Validation records are overwritable and machine-bound.** Severity low, confidence medium. `validate.py` in the routing-correction and product-roadmap folders overwrite `validation.json` and assert against `~/.codex/automations/...` and private backup directories. A rerun replaces the timestamp and hashes, and nobody else can run them. Acceptance: validators write attempt-numbered files and declare their ambient dependencies, or the record states "not reproducible off the author's machine".

**F8. Checkpoint text lives in one repository and one folder convention is unused.** Severity low, confidence high. Runtime `AGENTS.md` has no review checkpoint; `handoff.md` prescribes `reports/reviews/` while the first handoff sits under `reports/research/`. Acceptance: both AGENTS files carry the same checkpoint or the register's states it is register-only; the handoff path convention matches practice.

**F9. Subscription versus API billing for reviewer CLI calls is unproven.** Severity low, confidence medium. The metadata records list-price cost for two Fable 5.1 calls and no credential mode. Acceptance: future invocation metadata records the auth mode and the register's `new_paid_usage` claim cites it.

**F10. The heartbeat pause is a state file, not a control.** Severity low, confidence high. The pause is proven only by `automation.toml` at validation time; a resume from the Codex app would not be visible in the register until the next validator run. Acceptance: the execution state records the automation file's `updated_at` and the journey requires a dated entry for any status change.

**F11. Erasure claims rest on analogy plus unqualified physical deletion.** Severity low, confidence high. EDPB paragraph 104 and Ghost Vectors are quoted accurately; both transfer to ADRL's local ledger, NumPy index and backups only by mechanism, and the W3 evidence says physical, backup and active-copy erasure remain unqualified. Acceptance: the MEM-010 erasure check enumerates projection files and the newest backup and is run on a fixture session.

**F12. Sandbox CVEs are real but concern a different boundary.** Severity info, confidence high. Three of the four cited escapes are confirmed; they affect Claude Code's own sandbox, while ADRL's verifier sandbox is Seatbelt on macOS and its isolation candidate is a container. The vendor's "don't take a clean start as proof your settings loaded" line supports OPS-008's active probe. Acceptance: SAF-007's replacement names backend and version and includes the probe.

## Part D. URLs fetched versus search-only

Fetched and read (8 September 2026): arxiv.org/abs/2608.24358 and /html/2608.24358v1; /abs/2606.07587 and /html/2606.07587v1; /abs/2607.00053; /abs/2606.22902 and /html/2606.22902v3; /abs/2604.23530; /abs/2608.08239 and /html/2608.08239v1; /abs/2601.07206; /abs/2608.06867 and /html/2608.06867v1; /abs/2606.18497; /abs/2602.01757; /abs/2510.07429; platform.claude.com/docs/en/build-with-claude/thinking; platform.claude.com/docs/en/build-with-claude/compaction; code.claude.com/docs/en/llm-gateway-protocol; code.claude.com/docs/en/sandbox-environments; github.com/BerriAI/litellm/issues/7249 and /pull/7263; docs.litellm.ai/docs/proxy/response_headers; docs.litellm.ai/docs/proxy/auto_routing; edpb.europa.eu Guidelines 02/2025 v2.0 PDF (text extracted locally); learn.chatgpt.com/docs/config-file/config-reference; developers.openai.com/api/docs/guides/conversation-state; code.notdiamond.ai/docs/.

Search-only: the reasoning-block decoding incident (thehackernews.com 2026/08, labs.cloudsecurityalliance.org research note 20260814, implicator.ai, techtimes.com); CVE-2025-66479 (securityweek.com, oddguan.com, theregister.com); CVE-2026-39861 (advisories.gitlab.com, sentinelone.com); CVE-2026-55607 (advisories.gitlab.com, github.com advisory GHSA-7835-87q9-rgvv). The EDPB guidelines landing page returned 404; the PDF itself was fetched.

## Part E. Files opened versus skimmed

Opened in full (register): `reports/research/adrl-execution-state.json`; `reports/adrl-independent-research-review-2026-09-07.md`; `reports/research/adrl-independent-review-manifest-2026-09-07.json`; `reports/research/adrl-artifact-source-check-2026-09-07.json`; `BIBLIOGRAPHY.md`; `CHANGELOG.md` (all 7-8 September entries); `reports/adrl-course-correction-plan-2026-09-07.md`; `reports/adrl-adaptive-routing-rsi-blueprint-2026-09-08.md`; `reports/research/adaptive-routing-blueprint-2026-09-08/sources.json`; `reports/research/product-roadmap-2026-09-08/sources.json`, `validate.py`, `validation.json`, `validation-attempt-1.json`; `reports/adrl-routing-correction-2026-09-08.md`; `design/adrl-lab-planning-and-assessment-2026-09-08.md`; `design/adrl-context-graph-memory-proposal-2026-09-08.md`; `design/adrl-multi-harness-product-contract-2026-09-07.md`; `AGENTS.md`; `reports/adrl-wave-execution-template.md`; `reports/adrl-experiment-template.md`; `reports/research/independent-review-handoff-2026-09-08/README.md`, `skill-dispositions.md`, `skill-recheck.md`, `skill-recheck.json`, `skill-smoke-review.md`, `skill-smoke-review.json`, `skill-smoke-metadata.json`; `reports/research/routing-correction-2026-09-08/contract.md`, `validate.py`, `validation.json`, `checks-1/tests.log` (tail); `reports/research/routing-lab-2026-09-08/validation.json`, `run-1/QUALIFICATION.md`; `reports/research/launch-recovery-2026-09-08/run_probe.py` (header). Skills: `skill-claude/SKILL.md`, `skill-claude/references/handoff.md`, `skill-codex/agents/openai.yaml`, plus the live copies under `~/.claude/skills` and `~/.codex/skills` (diffed only).

Opened in full (runtime): `docs/responses-admission.md`; `docs/protocol-boundary.md`; `src/adrl/learning/artifacts.py` (lines 1-60 and 195-240); `tools/check_all.py` (lines 100-180).

Skimmed by grep or partial read: `reports/adrl-research-critique-2026-09-03.md` and `reports/research/critique-*-2026-09-03.md` (claim lines only); `adr/RTG/ADRL-RTG-001/002/003/004/007/008.md`, `adr/CAS/ADRL-CAS-004/005/006.md`, `adr/SEM/ADRL-SEM-004/007.md`, `adr/OPS/ADRL-OPS-001/006.md`, `adr/TRU/ADRL-TRU-002.md`, `adr/SAF/ADRL-SAF-007.md`, `adr/MEM/ADRL-MEM-005/010.md`, `adr/LRN/ADRL-LRN-002/003/004/007/008.md`, `adr/EVL/ADRL-EVL-001/003.md` and bucket READMEs (status, maturity, changelog and evidence lines); `reports/adrl-implementation-journey.md` (lines 55-115, 765-800 and grep); `reports/waves/w0-baseline.md`, `routing-decision-quality.md`, `lab-a-routing-experiment.md`, `lab-planning-deliverables.md`, `w3-active-copy-custody.md` (limit lines); `reports/adrl-w3-isolated-launch-2026-09-08.md`, `adrl-w3-2b2d2-launch-contract-2026-09-08.md`, `adrl-w3-transport-receipts-2026-09-08.md` (deviation lines); `reports/research/adrl-w3-isolated-launch-2026-09-08.json`, `adrl-w3-2b2d2-launch-contract-2026-09-08.json` (keys and run fields); `reports/adrl-independent-review-prompt-2026-09-08.md` (first 40 lines); `reports/adrl-product-roadmap-2026-09-08.md`, `adrl-startup-investment-brief-2026-09-08.md`, `adrl-executive-review-2026-09-08.md`, `adrl-routing-in-action-2026-09-08.md`, `adrl-lab-first-run-2026-09-08.md` (percentage and savings lines); runtime `tools/run_routing_lab.py`, `src/adrl/learning/readiness.py`, `src/adrl/app.py`, `src/adrl/cli/main.py`, `src/adrl/cascade/handoff.py`, `src/adrl/wire/observe.py` (lines 70-95), `src/adrl/proxy/pipeline.py`, `src/adrl/config/models.py`, `config/rungs.yaml`, `config/provider-pairs.yaml`, `docs/known-gaps.md`, `AGENTS.md` (grep only); register `reports/research/adrl-session-verifier-runner-2026-09-07.py`, `reproduce-verifier-experiment.py`, `dataset-validator-pilot-verifier-2026-09-07.py`, `routing-correction-2026-09-08/matrix_driver.py`, `routing-lab-2026-09-08/validate.py` (write targets only). Outside the snapshot, read-only: `~/.codex/automations/continue-adrl-implementation-waves/automation.toml`; directory listings of `~/projects/.adrl-execution-state/`, `~/Library/LaunchAgents`, `crontab -l`, `ps`.

Not opened: the 121-source 2 September bibliography targets, `source/` history, `output/`, `profiles/`, the W3 patches, the executive deck contents, and the runtime test suite (no test was run or read beyond grep for `block_binding` and `input_transformations`).
