# Taxonomy synchronization completion gate

User explicitly requests enforceable implementation-to-ADRL synchronization, optionally a skill. Scope: local development workflow only, no runtime behavior, model task, maturity promotion, automation, CI/remote setup or release change. Owners FND-005 and EVL-009 (scoped engineering gate, not changing runtime graduation blockers).

Implement a repo-owned adrl-taxonomy-sync skill mirrored to Codex and Claude, required by both repos AGENTS and wave closure template. Add a standard-library local CLI in register tools with begin and check: begin snapshots declared nonsecret engineering inputs in both dirty repos; check detects changes/additions/deletions since baseline, requires every changed implementation/tool/skill file mapped to owning existing ADR(s), requires dated evidence and links in each owning ADR plus matching INDEX, bucket and CHANGELOG entries, verifies pinned evidence hashes and reviewed semantic disposition, disallows maturity changes without separate referenced human record. Baselines include untracked inputs and preserve unrelated preexisting dirt. Register-wide historical stale grade audit is not silently claimed fixed. Output nonzero on missing/invalid/stale records. A check does not prove semantic correctness or become tamper-proof; source snapshot, review and required invocation provide layered discipline, not branch protection. Avoid altering runtime check_all; require a separate final closure command after ordinary checks so register updates can happen after runtime tests.

Minimum tests: complete fixture passes; unmapped changed/new/deleted input, stale evidence, missing ADR/index/bucket/changelog link, duplicate or missing index IDs and changed maturity without authority fail. Test already dirty baseline and current changes distinctly. Source scope/exclusions explicit; symlink escapes rejected. Non-runtime docs need no forced runtime rerun. Run against current repository for actual ownership evidence of this workflow. Source changes after check invalidate receipt. Human/reviewer statement remains attestational not cryptographic.

The skill must require snapshot before edits and map changes at close; missed baseline must be reported and reconstructed from real saved evidence, never fabricated after edits. A separate semantic reviewer judges ownership completeness, claim accuracy and maturity. Shared skill copies byte-identical. No approval requests for routine sync. No auto-editing decision wording or grades by checker. Preserve existing records and changes.

Challenge this bounded plan before implementation. Return blocking findings and a scoped disposition. Do not edit, execute tests, delegate or launch tools. You receive the full declared relevant instructions, engineering runner and owning ADRs below. This is not an all-ADR retrospective.

## Pre-review resolutions before implementation

B1: bootstrap-baseline.json and baseline-source contain a stdlib pre-edit capture of 422 inputs, created before any implementation edits. It is explicitly the bootstrap/reconstructed baseline; its original byte content is preserved. Git head/status are supplemental metadata captured now before edits, not the basis for attributing preexisting dirty files.
B2: closure JSON maps every changed input path to before/current hashes and ADR IDs; ownership is a reviewed author assertion for all input classes, not guessed from docstrings. Deleted file bytes are preserved in bootstrap snapshot; normal begin captures metadata/fields and callers preserve the snapshot under critical-review workflow.
B3: reports/reviews output is outside declared scanned inputs. A receipt must live outside both repo roots. Evidence/report inputs are explicitly pinned references, never the receipt itself. Review is hashed and final check is last.
B4/B5 alternative with concrete harness: tool stays in register/tools, tests in register/tests, Python stdlib unittest discovery and py_compile are the declared harness; optional available ruff check. No runtime source/tool edits in core, only AGENTS instruction additions. Existing full-runtime results are dated, not claimed to match all current inputs after AGENTS changes. Compare runtime-relevant inputs separately against the recorded W0 manifest and do not blanket-classify artifacts as nonruntime. No engine suite is needed to validate an independent stdlib doc checker. This avoids a coupling where adrl-core cannot be tested without a personal skill installation.
B6: read actual INDEX/bucket formats: index has Review verdict and abbreviated/historical maturity, not a canonical Status column. This scoped forward checker does not claim to fix RV12/23 historical inconsistencies. Require unique stable ADR coverage and new dated sync markers linking the same pinned evidence in owning ADR, ADR changelog, actual INDEX row, actual bucket row and root CHANGELOG section. Also require exact current Status/Maturity/Review verdict values in closure and reject changes without a pinned human record containing old/new fields, ADR id, name/date. A semantic reviewer explicitly checks no contradictory new claims; full register reconciliation remains separate and open.
B7: all changed files in declared scope require ownership map except derived register files, which are restricted to named owners/owning buckets/INDEX/CHANGELOG and also hash-pinned in closure. Stray ADR edits fail, including grade changes. Exclusions list reports output, .project/runtime/secrets/.git and historical source (not modified here); changes to excluded material require explicit scope expansion or separate reviewed disposition. Checker coverage is declared, not a claim to police every filesystem path.
Nonblocking: missing/pending semantic review fails; mirror copies must match canonical bytes; inherited runtime freeze limits explicit. No new grade or status changes.
