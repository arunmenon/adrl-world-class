# Taxonomy synchronization workflow

Local completion tooling for ADRL-FND-005 and ADRL-EVL-009. Before/after declared input snapshots, explicit changed-file ownership, dated ADR/index/bucket/changelog evidence mapping, field-change disposition references, pinned semantic review, source drift and installed-skill parity are checked. Global INDEX ID coverage and affected bucket IDs are checked; historical unrelated bucket/status/maturity conflicts are not normalized. Initial actual scan found CAS-009 missing from its bucket table; this preexisting hygiene issue remains open and does not get silently fixed or treated as passing full-register reconciliation.

The canonical skill is installed identically for Codex and Claude and required by both repositories' AGENTS.md plus the wave template. The CLI fails nonzero on missing/stale closure requirements and writes successful receipts outside the repositories. A receipt must be rerun after relevant changes. No automatic CI hook or remote branch protection was installed. A caller can skip a local command; semantic review and human identity assertions are not authenticated by the script. Source scope and excluded reports/history/private data are explicit. Markers and hashes do not prove prose correctness.

Validation: focused stdlib tests exercise valid closure, already-dirty baseline, missing ownership, additions/deletions, evidence drift, register mapping gaps, duplicate INDEX, changed maturity, pending review, post-review drift, mirror drift, symlinks and path escape. [Raw test results](tests.log). Core runtime code/config/tests/tools are unchanged relative to this wave baseline; core AGENTS changed, so the old full-source runtime receipt is not represented as matching every current input. No runtime suite rerun, model task, experiment-budget consumption, automation restart or maturity change.

Fable pre-review and recorded resolutions are in [packet](packet.md) and [original critique](pre-review.md). Post-review disposition and a copy of the final outside-repository self-application receipt are separate artifacts in this directory. A pending review is not a passed gate.

Material-fix evidence: [provenance and protected display comparison](provenance-recheck.json) verifies the preserved 422-input pre-edit capture, unchanged owning summary grade/verdict cells, and identical installed skill hashes. Compared with the actual latest RV-01 checks-2 manifest (324 inputs), only core AGENTS.md differs; all other 323 inputs match. This is not a claim that the old complete source receipt still applies. Scope/parser fields were reconstructed from preserved bytes; no stand-alone baseline script existed at capture time, and no script hash is invented.

The checker now requires the dated ADR changelog marker to be inside its table and protects owning INDEX/bucket verdict and maturity cells against unrecorded changes. Historical discrepancies remain open. Focused tests include a valid human-record case, standalone changelog rejection, display-grade rejection, scope mismatch and CLI output rules. Installed parity was independently measured before the final check; final closure will also exercise its own mirror check after review acceptance.

## Scoped review disposition

Fable's [material-fix recheck](recheck.md) accepts this scope, conditional on unchanged reviewed inputs and a final passing check. Nonblocking residuals: display-field protection compares owning rows only; unrelated historical summary-cell conflicts are not reconciled. The pre-edit bootstrap/derived baseline lives under excluded reports as a disclosed one-time bootstrap exception; normal begin requires outside-repository output. Existing historical changelog ordering and root changelog rendering remain minor limitations. These do not establish broader register reconciliation. The outside-repository final receipt and its copy provide actual mirror-check execution evidence; this report does not itself replace that receipt.
