# Claude Code ROI: compatibility preparation

We have identified why the first real routing test cannot run unchanged, and Fable independently challenged the plan. No ADRL-controlled model task has run in this preparation; savings remain unproved.

The useful next product increment is small: let ADRL select another Claude model while preserving Claude Code's request features, then show which model actually answered. Existing code removes thinking and output settings on cheaper-tier rewrites, all configured Claude IDs sit in frontier, and normal LIVE loading rejects unqualified rungs. These are reproduced facts, not a demonstrated subscription restriction.

Fable recommends testing Opus and Sonnet within the existing Claude tier first, which avoids prematurely claiming that the cheaper tier is qualified. We accept that narrower candidate. We do not accept silently activating rewrites in SHADOW; any experiment control must be explicit. Its possible header concern reproduced native session-ID forwarding, not a credential leak. Its tool-removal claim was qualified: native Read survives the existing filter.

The next implementation scope is model-only Claude selection, truthful direct-endpoint attribution, enforced request limits and an explicit isolated experiment profile. Initial selection is the immediate ROI prerequisite; mid-session switching is a separate capability. Then we can run the bounded compatibility check and, if it works, the frozen-policy quality/usage comparison. A forced selection proves control, not that the router makes good choices.

The proposed eight-request compatibility budget and later 18-attempt coding matrix are not active allowances. Existing task-arm limit remains ten. Native subscription login is present, but paid-overflow and exact experimental access still need disposition before launch. Automation remains paused.

Preparation did not change runtime/configuration or ADR decisions and maturity fields: all 425 declared source inputs match the preflight baseline. Twenty-two selected source snapshots match their originals. No runtime suite rerun is claimed; evidence consists of static inspection and synthetic function probes. Reviewer usage occurred separately from ADRL task execution; CLI list-price metadata is not an invoice.

Read [the proposed packet](packet.md), [original Fable critique](pre-review.md), [dispositions and next scope](dispositions.md), [static probes](static-probes.json), [header probe](header-probe.json), [source verification](source-verification.json) and [reviewer provenance](invocation.json). The packet is a preserved reviewed proposal; dispositions record narrower candidates and unresolved issues. P1 and the live-launch gate remain open.
