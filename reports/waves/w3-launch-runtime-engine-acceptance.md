# Bounded runtime launch acceptance

Frozen 8 September 2026 before fresh daemon mutation, after the offline launch/ownership fault
cases passed. This exercises the runtime coordinator, not the earlier research driver. Previous
research allowances stay closed. Owners and invariants follow the
[launch event contract](w3-launch-event-contract-v1.md).

Use one imported self-authored image from the verified retained tar, exact single-layer diff-ID,
fixed /probe forced_stop command and the bundled public seccomp profile. Pin the existing local
Docker 27.3.1/API 1.47 engine, explicit socket and supported capability profile. No image pull,
install/build, network, host mounts, daemon configuration, real task payload, model call or spend.
All active fixtures have the known internal seven-second exit timer, subject to OS scheduling.

At most two focused four-case invocations, a second only after a diagnosed failure and changed
code/evidence; at most one required combined eleven-check invocation with engine cases enabled.
Maximum 16 create-issued records total and one image import. With no repair, four focused launch
cases plus four launch and four stopped-owner cases in the combined run consume 12 containers,
eight successful starts. Unused headroom grants no unchanged retry. Stop on unexplained identity
or cleanup failure. Keep at most three program repair cycles; preserve every failed result.

The four runtime cases are known start/stop/retained layer then discard; lost start acknowledgement
then owned abort; content-key erasure after daemon startup then host-authenticated cleanup; and
abrupt owner-process death after startup, with persisted claim and reopened operator discard.
Each must retain the permanent marker/fence, forbid another launch and keep exact-close/learning
false. Observe actual worker failure separately from successful cleanup. Stopped-owner compatibility
cases retain their old scope and do not start their resources.

Record/fsync create intent before the exact create and the returned ID immediately afterward.
No name adoption/reissue or broad cleanup. Test cleanup can use only the original exact receipt;
runtime force-discard requires authenticated execution history and the exact execution projection.
Confirm all owned IDs absent and remove the original uniquely tagged/labelled image before closing.
Import timeout 15 seconds; HTTP I/O <=2 seconds/131072 response bytes, ledger wait <=15 seconds,
fixture owner subprocess <=5 seconds, execution run <=8 seconds and stop observation <=2 seconds.
These bound intended operations, not OS scheduling or hard real-time completion.

Before import, preserve a full source manifest and the exact test, policy, fixture, contract and
setup hashes with the passing offline output. The combined check runner records before/after
hashes. Runtime metadata inventory, ADR module map and all eleven checks must pass before this
implementation slice closes. No full-W3, independent-security, real-harness or maturity promotion.

## Diagnosed repair before second focused invocation, 2026-09-08

First focused run: 3 passed, 1 failed. The erasure case reached absent with false eligibility,
then its final relaunch-denial check expected the wrong exception class. The ledger correctly
wrapped key-unavailable refusal as LedgerAppendFailure. All four originally receipted resources
were cleaned and confirmed absent. No engine identity or runtime cleanup failure occurred.

The changed run broadens that test's accepted denial types, preserving the real denial. Before
retry, self-review also adds independent recovery serialization and an issued-discard no-reissue
control, with its policy/filesystem inventory and contract refinement. Preserve the first 315
executed source inputs and freeze the new source hashes after the updated offline cases pass.
The original maximum remains two focused invocations, one combined run, 16 containers and one
image. No new import, source/payload exposure or allowance expansion is authorized.

## Closed batch and one-off operator cleanup disposition, 2026-09-08

The second and final focused invocation also finished 3 passed/1 failed: its first case lost
an original create receipt, leaving resource_create_uncertain before launch. The underlying
exception type was not retained, so timeout is only a hypothesis. The runner lacked fail-fast
and executed the remaining three cases; this departed from the stop-on-unresolved-ownership
instruction. Record that deviation and add durable uncertainty accounting/fail-fast before any
future engine batch. No third focused run or combined engine run is allowed under this packet.

Seven returned creation receipts have exact absence confirmations. Read-only inspection of the
single name from the eighth fsynced create intent found a never-started synthetic candidate:
`d62cb59e04cd3bcf45a657b0dd05399f1fedef2f9949c02912e43a00041a4fda`.
It matches the original unique operation name, this batch's uniquely imported image and every
requested command/security/resource control. Its creation timestamp falls within the recorded
second invocation. It contains only the self-authored fixture and has never executed.

Under the user's existing authorization for routine local engineering and disposable-fixture
cleanup, perform one explicitly recorded operator-maintenance exception: revalidate those facts,
remove that exact stopped candidate non-force and confirm absence, then remove the original
uniquely tagged/labelled image. This exception to the packet's client-receipt-only cleanup rule
is not runtime ownership adoption, successful creation binding, general name-based recovery,
new launch authority or engine qualification. Preserve the original unreceipted uncertainty.
No real payload, host mount or model data is involved. Further engine work stays stopped.
