# Lab A.1: inspect actual routing and dispatch in a synthetic workbench

8 September 2026. Bounded foreground restart following “What can we start now?”
The hourly implementation automation remains paused. This packet authorizes only
the offline diagnostic slice below; it does not dispose of other roadmap gates.

## Product outcome

Run a repeatable suite and inspect task/context, actual decision, dispatched request,
receipt and failure together. Preserve known mixed-intent weaknesses so a later
routing correction can be measured against this baseline. Do not change policy.

## Ownership and frozen scope

Primary EVL-005; supporting RTG-002, MEM-001 and SEM-007 for actual decisions,
isolated synthetic evidence and explicit protocol scope. Existing gates and cascade
run through the real composition root. Only the input harness and response endpoint
are synthetic. The tool is a developer diagnostic, not a new public product API.

Use a frozen synthetic suite with six task families, same-task context variants,
continuation/escalation, a synthetic secret pin, a blocked request and an upstream
failure. Record receipt provenance, missing identity, every planned attempt and
interrupted attempts. No expected answer or success label is generated.

Create all runtime stores in disposable lab directories; never write synthetic
outcomes into a developer ledger. Persist synthetic-only diagnostic exports with
learning eligibility false, exact source/config/suite fingerprints, fixture identity
and real-harness/model versions explicitly absent. No provider or harness executes.

## Baseline and limits

Verified backup: `/Users/arunmenon/projects/.adrl-execution-state/lab-a-20260908T081840Z/manifest.json`
preserves 409 files across both repositories. All 316 declared runtime inputs match
the previous full build. Original routing diagnostic cases/results remain unchanged.

No model calls, network providers, paid usage, Docker/engine mutation, installation,
live developer exposure, learned authority, commits or scheduled continuation.
Keep the config evidence bypass confined to the in-process synthetic transport,
as in existing integration tests; it is never a production admission mechanism.
At most three failed repair cycles. Record failed runs and their cause.

## Exit and follow-up

Meaningful runner/accounting regression tests and all eleven engineering checks
pass on stable source. Engine tests without a fresh allowance stay skipped and are
reported. Publish an intuitive result table and raw trace links. Synchronize owners,
bucket overviews, index, changelog, roadmap, journey and execution state. No formal
status/maturity promotion, quality/savings claim or full Lab A completion claim.

Next, correct mixed-intent routing using the preserved baseline. Real harness Lab B
still requires custody/exact-output qualification, a concrete harness profile,
verifier, finite execution matrix and approved access/spend. K0 and RSI follow
qualified outcomes; this packet supplies no learned knowledge checkpoint.
